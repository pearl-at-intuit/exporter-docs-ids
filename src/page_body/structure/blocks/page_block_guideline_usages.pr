{[ let block = context /]}

<div class="content-block content-block--guidelines-usage layout-columns-{{block.numberOfColumns}}" id="{{ getSearchIDString(block.id) }}">
  {[ for item in block.children ]}
    {[ inject "page_block_guideline_usage" context ({"item": item}) /]}
  {[/]}
</div>